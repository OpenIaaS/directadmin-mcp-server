# Runtime — pin floors. mcp 2.x removed FastMCP; stay on the 1.x SDK.
# Floors raised in 2.7.0 to versions that contain the 2024 multipart / redirect
# security fixes (starlette 0.40+, python-multipart 0.0.18+, httpx 0.28+).
fastapi>=0.115.0
uvicorn[standard]>=0.27.0
starlette>=0.40.0
pydantic>=2.6.0
pydantic-settings>=2.2.0
python-dotenv>=1.0.1
httpx>=0.28.0
mcp>=1.6.0,<2.0.0
python-multipart>=0.0.18
typing-extensions>=4.16.0
